name: E2E Agent Test
on: workflow_dispatch

jobs:
  test-windows:
    runs-on: [self-hosted, windows, desktop-test]
    timeout-minutes: 15
    env:
      CDP_PORT: "9333"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Kill stale processes
        shell: powershell
        run: |
          Stop-Process -Name node -Force -ErrorAction SilentlyContinue
          Stop-Process -Name electron -Force -ErrorAction SilentlyContinue
          Stop-Process -Name "OpenAgents Launcher" -Force -ErrorAction SilentlyContinue
          Write-Host "Killed stale processes"
        continue-on-error: true

      - name: Clean state
        shell: powershell
        run: |
          Remove-Item "$env:USERPROFILE\.openagents\daemon.*" -Force -ErrorAction SilentlyContinue
          Remove-Item "$env:USERPROFILE\.openagents\daemon.yaml" -Force -ErrorAction SilentlyContinue
          Remove-Item "$env:USERPROFILE\.openagents\nodejs" -Recurse -Force -ErrorAction SilentlyContinue
          Write-Host "State cleaned"

      - name: Install Node.js and openagents CLI
        shell: powershell
        run: |
          $env:PATH = "$env:USERPROFILE\.openagents\nodejs;$env:APPDATA\npm;$env:PATH"
          irm https://raw.githubusercontent.com/openagents-org/openagents/develop/install.ps1 | iex

      - name: Setup Electron dev folder
        shell: powershell
        run: |
          $env:PATH = "$env:USERPROFILE\.openagents\nodejs;$env:APPDATA\npm;$env:PATH"
          $dir = "$env:USERPROFILE\Desktop\launcher-e2e"
          Remove-Item $dir -Recurse -Force -ErrorAction SilentlyContinue
          New-Item -ItemType Directory -Path $dir -Force | Out-Null
          Copy-Item -Path "packages\launcher\src" -Destination "$dir\src" -Recurse
          Push-Location $dir
          npm init -y 2>$null | Out-Null
          npm install @openagents-org/agent-launcher@latest electron@33 2>&1 | Select-Object -Last 3
          $pkg = Get-Content package.json | ConvertFrom-Json
          $pkg.main = "src/main/main.js"
          $pkg | ConvertTo-Json -Depth 10 | Set-Content package.json
          Pop-Location
          Write-Host "Launcher dev folder ready at $dir"

      - name: Launch Electron with CDP
        shell: powershell
        run: |
          $env:PATH = "$env:USERPROFILE\.openagents\nodejs;$env:APPDATA\npm;$env:PATH"
          $dir = "$env:USERPROFILE\Desktop\launcher-e2e"
          Start-Process -FilePath "npx" -ArgumentList "electron . --remote-debugging-port=$env:CDP_PORT --remote-allow-origins=*" -WorkingDirectory $dir -WindowStyle Normal
          Start-Sleep -Seconds 10
          # Verify CDP is up
          try {
            $response = Invoke-WebRequest -Uri "http://127.0.0.1:$env:CDP_PORT/json" -UseBasicParsing -TimeoutSec 10
            Write-Host "CDP ready: $($response.Content.Substring(0, 100))..."
          } catch {
            Write-Host "CDP not ready: $_"
            exit 1
          }

      - name: Install Playwright
        shell: powershell
        run: |
          $env:PATH = "$env:USERPROFILE\.openagents\nodejs;$env:APPDATA\npm;$env:PATH"
          npm install -g playwright@latest 2>&1 | Select-Object -Last 3

      - name: Run E2E test via Playwright
        shell: powershell
        env:
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_BASE_URL: ${{ secrets.LLM_BASE_URL }}
          LLM_MODEL: ${{ secrets.LLM_MODEL }}
          WS_TOKEN: "Ux8TWAXT_YP0WUmWjtmDzjtcnZChGVZ7hYCT9ddAzrE"
        run: |
          $env:PATH = "$env:USERPROFILE\.openagents\nodejs;$env:APPDATA\npm;$env:PATH"
          node infra/e2e-launcher-test.js

      - name: Collect logs
        if: always()
        shell: powershell
        run: |
          Write-Host "=== DAEMON LOG ==="
          Get-Content "$env:USERPROFILE\.openagents\daemon.log" -ErrorAction SilentlyContinue
          Write-Host "=== STATUS ==="
          Get-Content "$env:USERPROFILE\.openagents\daemon.status.json" -ErrorAction SilentlyContinue

      - name: Cleanup
        if: always()
        shell: powershell
        run: |
          Stop-Process -Name node -Force -ErrorAction SilentlyContinue
          Stop-Process -Name electron -Force -ErrorAction SilentlyContinue
